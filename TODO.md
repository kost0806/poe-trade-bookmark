# 할 일

리그 교체를 견디는 일과, 설계하다 발견한 구현·요구사항의 어긋남을 모아 둔 목록입니다.
각 항목은 증상 · 원인(파일:줄) · 고칠 방향만 적고 코드는 쓰지 않습니다. 기능을 손대기
전에 [docs/REQUIREMENT.md](docs/REQUIREMENT.md) → `docs/DESIGN.md` 순으로 먼저
고칩니다(CLAUDE.md). 순수한 버그 수정은 예외입니다.

## 우선순위 기준

기준은 **새 리그가 열린 날 사용자가 틀린 결과를 보는가**입니다. `⚠ 확인 필요`가 붙은
것은 실제 거래소에서 재 봐야 방향이 확정됩니다.

| 등급 | 뜻 |
| --- | --- |
| P0 | 새 리그가 열린 날 바로 사용자를 틀리게 만든다 |
| P1 | 그 주 안에 하지 않으면 갱신이 헛돌거나 사고를 못 잡는다 |
| P2 | 다음 리그까지 미뤄도 된다 |

---

## 1. 리그 교체 대응

### T9. 거래소가 모르는 stat id를 무시하는지 확인되지 않았다
- 증상: 조용히 무시한다면 검색은 성공하는데 그 모드만 안 걸러지고, 인게임 정규식은 계속 숨겨 "거래소는 안 걸러 주는데 인게임만 숨기는" 어긋남이 그대로 재현된다. 실패로도 안 드러나므로 사용자가 알아챌 길이 없다.
- 원인: 거절하는 경우는 이제 400으로 갈라 안내한다(`panel.js:1023-1025`). 무시하는 경우는 애초에 실패가 아니라서 안내로는 잡히지 않는다. 실패 응답 본문도 여전히 `res.ok`일 때만 읽는다(`panel.js:1059-1064`).
- 고칠 방향: V2를 실물에서 잰다. 무시한다면 고칠 곳은 안내가 아니라 `docs/DESIGN.md` §10.3 5단계 추출 명령의 검사다. 거절한다면 실패일 때도 `body.error?.message`를 읽어 덧붙인다 — 거래소가 거부한 id를 알려 준다면 그 문구가 곧 갱신할 자리를 가리킨다.
- 우선순위: P1. ⚠ 확인 필요: 모르는 stat id를 거절하는지 무시하는지(V2). 무시한다면 P0로 올라간다.
- 요구사항: FR-BUILD-12는 충족. NFR-LEAGUE-04의 남은 부분.

### T10. 지난 리그의 북마크·기록이 구분되지 않는다
- 증상: 목록에 지난 리그와 현재 리그 항목이 똑같이 섞여 있다. 리그 이름은 메타 줄에 붙지만(`panel.js:361`·`484`) API의 영문 id라 드롭다운의 현지 이름과 표기도 다르다. 눌러 들어가면 조건이 안 뜨는데 사이드바는 "이미 저장된 검색입니다"라고 말한다(`panel.js:642`).
- 원인: 확장이 리그의 유효 기간을 모른다. `loadLeagues`의 결과는 빌더만 쓰고 목록 렌더링(`panel.js:350`·`476`)은 참조하지 않는다.
- 고칠 방향: 리그 목록을 목록 렌더링에서도 쓰고, `bookmark.league`가 목록에 없으면 메타 줄에 `· 지난 리그`를 붙인다. 지우지는 않는다. 목록을 아직 못 받았으면 아무 표시도 하지 않는다. 초기화 순서가 걸린다 — `renderList`(`panel.js:1464`)가 `initBuilder`(`panel.js:1467`)보다 먼저 돈다.
- 우선순위: P2 · ⚠ 확인 필요: 만료된 검색 ID와 없어진 리그에서 거래소가 무엇을 그리는지(V3) · 미충족: NFR-LEAGUE-05 — "지난 리그의 북마크·기록·프리셋이 남아 있어도 오동작하지 않고, 만료된 검색은 사용자가 그 사실을 알아볼 수 있어야 한다."

---

## 2. 나중에 생각해 볼 것

- **프리셋 내보내기/가져오기.** 지금은 `chrome.storage.local`에만 있어 다른 컴퓨터로 옮길 방법이 없습니다(REQUIREMENT §8의 "프리셋 이동 불가"). JSON 한 덩어리를 텍스트로 주고받는 정도면 파일 권한 없이도 됩니다.
- **북마크·기록까지 함께 내보내기.** 프리셋만 옮기면 반쪽이라 같은 자리에 붙이는 편이 자연스럽습니다.
