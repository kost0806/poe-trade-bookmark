# 남은 일

## 실물로 확인할 것 (한글)

전에 적어 둔 두 가지(맵모드 프리셋 저장, 8모드 검색의 희귀도 조건)는 구현했습니다.
남은 것은 **실제 거래소에서 한 번 확인하는 일**뿐입니다. 확장을 다시 로드하고
`poe.kakaogames.com/trade`에서 아래를 눌러 보면 됩니다.

| 확인할 것 | 어긋나면 고칠 곳 |
| --- | --- |
| 모드 고르기 창이 거래소 위에 제대로 뜨는지 | `panel.css`의 `.modal` — 화면 기준(`position: fixed`)이라 밀어낸 여백·`overflow`에 안 걸리게 해 두었습니다 |
| 검색 조건 요약이 실제 마크업에서 읽히는지. 특히 드롭다운 필터의 라벨(`타락함: 아니오`) | `search-summary.js` 위쪽 선택자 네 줄 |
| 프리셋 저장 → 다시 선택 → 삭제 | `panel.js`의 프리셋 절 |
| 8모드 검색 결과에 고유 지도가 안 섞이는지 | `trade-query.js`의 `RARITY_NON_UNIQUE` |

## 실물로 확인할 것 (영문)

헤드리스 크롬으로 두 언어의 사이드바·모드 창·언어 바꾸기까지 그려 확인했지만,
아래 둘은 실제 게임과 거래소에서 봐야 합니다.

| 확인할 것 | 어긋나면 고칠 곳 |
| --- | --- |
| 영문 클라이언트 창고에 정규식을 붙여넣었을 때 고른 모드의 지도만 숨는지 | `map-mods-en.js`의 키워드 — 고치지 말고 `tools/build-map-mods-en.js`를 고쳐 다시 만듭니다 |
| `www.pathofexile.com/trade`에서 검색 조건 요약이 읽히는지 | `search-summary.js` — 폼 마크업이 언어와 무관하다고 보고 있습니다 |

영문 키워드를 더 단단히 검사하려면 **영문 클라이언트에서 T16 희귀 지도 하나를
Ctrl+C** 해서 `test/fixtures/en/map-item-lines.json`에 넣어 주세요. 지금 그 파일은
공개된 이슈에서 옮겨 온 것이라 '걸리면 안 되는 줄'로만 쓰고 있습니다
(`data/en/README.md` 참고).

영문 거래소 API 원본(`/api/trade/data/*`)을 직접 쓰는 컴퓨터에서 받아 두면 대조표를
한글과 같은 기준으로 맞출 수 있습니다. 받는 법은 `data/en/README.md`에 있습니다.

`node --test`(85개)와 `node --check`는 통과 상태입니다.

## 나중에 생각해 볼 것

- 프리셋을 파일로 주고받기(내보내기/가져오기). 지금은 `chrome.storage.local`에만 있어서
  다른 컴퓨터로 옮길 방법이 없습니다.
- 대량거래(`/trade/exchange/`)는 폼 구조가 달라 검색 조건 요약이 비어 있습니다.
  기록에는 검색 ID 이름으로 남습니다.
- 언어가 둘뿐입니다. 거래소는 러시아어·일본어 등도 있는데, 맵모드 목록을 그 언어로
  만들려면 `tools/`의 두 스크립트에 poedb 언어 코드만 더하면 됩니다(문구 표는 손으로
  적어야 합니다).
