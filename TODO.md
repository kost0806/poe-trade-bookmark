# 할 일

리그 교체를 견디는 일과, 설계하다 발견한 구현·요구사항의 어긋남을 모아 둔 목록입니다.
각 항목은 증상 · 원인(파일:줄) · 고칠 방향만 적고 코드는 쓰지 않습니다. 기능을 손대기
전에 [docs/REQUIREMENT.md](docs/REQUIREMENT.md) → `docs/DESIGN.md` 순으로 먼저
고칩니다(CLAUDE.md). 순수한 버그 수정은 예외입니다.

## 우선순위 기준

기준은 **새 리그가 열린 날 사용자가 틀린 결과를 보는가**입니다. `⚠ 확인 필요`가 붙은
것은 실제 거래소에서 재 봐야 방향이 확정됩니다.

| 등급 | 뜻 |
| --- | --- |
| P0 | 새 리그가 열린 날 바로 사용자를 틀리게 만든다 |
| P1 | 그 주 안에 하지 않으면 갱신이 헛돌거나 사고를 못 잡는다 |
| P2 | 다음 리그까지 미뤄도 된다 |

---

## 1. 리그 교체 대응

### T1. 리그 목록 캐시를 비울 방법이 없다
- 증상: 새 리그 첫날, 어제 거래소를 연 사용자는 최대 24시간 동안 빌더에서 새 리그를 고를 수 없다.
- 원인: `panel.js:888-893` 캐시 신선도만 본다. `LEAGUE_CACHE_KEY`(`panel.js:12`)를 **지우는 코드가 저장소에 없다**(`898`에서 쓰기만 한다).
- 고칠 방향: 리그 드롭다운 옆에 새로고침 단추를 두고, `loadLeagues`가 `{ force }`를 받아 캐시 검사를 건너뛰게 한다. 연타 방지는 `SEARCH_COOLDOWN_MS`(`panel.js:723`)와 같은 방식.
- 우선순위: P0 (NFR-LEAGUE-02 직결) · 요구사항 신설 필요: FR-BUILD-13 — "리그 목록을 사용자가 직접 다시 받을 수 있다. 캐시를 비우고 드롭다운을 다시 그린다."

### T2. 저장해 둔 리그가 사라지면 조용히 다른 리그가 선택된다
- 증상: 지난 리그를 골라 두었던 사용자가 새 리그 첫날 빌더를 열면 안내 없이 첫 항목이 선택되고, 그대로 엉뚱한 리그에 검색이 등록된다.
- 원인: `panel.js:1274` — 저장분이 목록에 없으면 아무 일도 안 하고, `<select>`는 첫 `<option>`을 값으로 갖는다.
- 고칠 방향: 목록에 없을 때를 분기로 만들어 `setBuilderStatus`로 바뀐 사실을 알리고, `saveBuilderState()`로 저장분도 갈아 둔다(안 그러면 매번 같은 안내가 뜬다).
- 우선순위: P0 · ⚠ 확인 필요: `/api/trade/data/leagues`의 정렬 순서(첫 항목이 무엇인지) · 요구사항 신설 필요: FR-BUILD-14 — "저장해 둔 리그가 목록에 없으면 바뀐 사실을 알리고 저장분을 갱신한다."

### T3. 빌더의 고른 개수와 실제로 걸리는 모드가 어긋난다
- 증상: 모드가 없어지거나 id가 바뀌면 버튼에는 `12개 거름`인데 실제 검색·정규식은 11개만 거른다. 프리셋과 달리 아무 경고가 없다.
- 원인: `panel.js:1258`이 저장된 키를 `MAP_MODS`와 대조 없이 되살린다. 개수는 `selected.size`(`869`·`874`·`876`)로 세는데 실제로 쓰는 것은 `selectedMods()`(`panel.js:760`, `865`·`916`)라 두 수가 갈라진다.
- 고칠 방향: 복원할 때 `applyPreset`과 같은 검사를 한다(`panel.js:1168-1173`) — 없는 키는 넣지 않고 개수를 알린 뒤 `saveBuilderState()`로 정리. 개수 표시도 `selectedMods().length`로 바꿔 갈라질 자리를 없앤다.
- 우선순위: P0 (NFR-LEAGUE-04, FR-BUILD-11 보강) · 요구사항 신설 필요: FR-BUILD-16 — "복원한 선택 중 모드 목록에 없는 것은 빼고 몇 개를 뺐는지 알린다."

### T4. 낡은 폴백 리그 이름이 쓰인 사실이 보이지 않는다
- 증상: 리그 API가 실패하면 끝난 리그(`올플레임`)와 `Standard`만 뜨고, 그것이 진짜 목록인 줄 안다. 그대로 만들면 **"검색 실패 (HTTP 404). 거래소 로그인 상태를 확인하세요."** 가 떠 멀쩡한 로그인을 의심하게 된다.
- 원인: `panel.js:721` `FALLBACK_LEAGUES`와 `trade-query.js:44` `DEFAULT_LEAGUE`에 리그 이름이 박혀 있고, `panel.js:901`은 폴백을 썼다는 흔적을 남기지 않으며, `panel.js:936`은 모든 실패를 로그인 문제로 안내한다.
- 고칠 방향: ① `loadLeagues`가 폴백 여부를 알리고 `initBuilder`가 그때 안내를 띄운다 ② `panel.js:936`을 상태 코드별로 나눈다(401·403 로그인 / 404 리그 이름 / 400 조건) ③ 두 파일에 나뉜 리그 상수를 한쪽으로 모은다(`DEFAULT_LEAGUE`는 `trade-query.js`의 export에도 없어 테스트가 닿지 못한다).
- 우선순위: P0. ⚠ 확인 필요: 없는 리그로 POST했을 때의 상태 코드(V1).
- 요구사항 신설 필요: FR-BUILD-15 — "폴백 리그 목록을 쓰면 그 사실을 알린다." FR-BUILD-12 보강(상태 코드별 구분).

### T5. 빈 리그 목록이 24시간 캐시된다
- 증상: `realm` 표기가 바뀌면 필터 결과가 빈 배열이 되고, 드롭다운이 빈 채 24시간 고정된다. 그동안 모든 검색이 박아 둔 끝난 리그로 나가는데 화면에는 아무 이유도 안 뜬다.
- 원인: `panel.js:897-899` — 빈 배열도 "성공"으로 캐시한다. 이어서 `panel.js:915`의 `leagueEl.value || DEFAULT_LEAGUE`가 조용히 발동한다.
- 고칠 방향: 필터 결과가 비면 캐시하지 않고 `FALLBACK_LEAGUES`로 넘긴다(T4의 안내가 함께 뜬다). 캐시를 읽을 때도 비어 있으면 무시하고 다시 받는다.
- 우선순위: P1 · 요구사항 신설 필요: NFR-API-06 — "빈 리그 목록은 캐시하지 않는다."

### T6. 픽스처에 어느 리그·언제 받은 것인지가 없다
- 증상: 리그가 두어 번 지나면 `test/fixtures/`가 갱신된 것인지 알 근거가 없다. `node --test`는 사본끼리 맞춰 보므로 통과해도 무엇을 보증하는지 알 수 없다.
- 원인: 픽스처 JSON에 시점·출처 필드가 없고, `test/fixtures/README.md`도 출처만 적고 시점을 적지 않는다.
- 고칠 방향: 우선 `test/fixtures/README.md`와 `data/README.md`에 파일별 "받은 리그 / 받은 날 / 출처 URL" 표를 둔다. 더 강하게 하려면 `test/fixtures/meta.json`에 모아 테스트가 그 값을 출력하게 한다.
- 우선순위: P1 (NFR-LEAGUE-04) · 요구사항 신설 필요: NFR-DEV-06 — "픽스처는 어느 리그의 언제 받은 것인지를 함께 기록한다."

### T7. 갱신 절차가 저장소에 없다
- 증상: `data/README.md:31`과 `test/fixtures/README.md:67`·`93`이 "리그가 바뀌면 다시 받아야 합니다"라고만 적고 **어떻게** 뽑는지가 없다. 규칙은 자명하지 않다 — `map-item-names.json`은 `search-items.json`의 지도 묶음에서 `text`가 아니라 `type`을 모은 것이고, `trade-stats.json`은 `map-mods.js`의 ids에 `AFFIX_COUNT_ID`·`INFLUENCE_IDS`(`trade-query.js:10`·`19`)를 더한 것이다.
- 원인: 절차 문서도 스크립트도 없다. 설계 문서 자체가 아직 저장소에 없다(T12 참조).
- 고칠 방향: `docs/design/`에 리그 교체 플레이북을 넣고 추출 명령을 그대로 적는다. `node -e` 한 줄이라 빌드 단계가 생기지 않아 "의존성을 추가하지 않는다" 규칙과 어긋나지 않는다. `trade-query.js`의 영향력 id를 고치면 명령도 함께 고쳐야 한다는 점을 적어 둔다.
- 우선순위: P1 (NFR-LEAGUE-03) · 요구사항 신설 필요: NFR-DEV-07 — "리그 데이터 갱신 절차와 그 검증 방법을 문서로 둔다."

### T8. 기본 프리셋의 stat id를 검사하지 않는다
- 증상: `presets.js`의 키가 하나 낡으면 사용자가 프리셋을 고른 순간에야 "…1개는 건너뜀"으로 드러난다. 저장소의 데이터 오류를 사용자가 대신 발견한다.
- 원인: `test/`에 `presets.js`를 읽는 테스트가 없다. 키는 `modKey`(`panel.js:759`)와 글자 단위로 같아야 하는데 그것을 지키는 장치가 없다.
- 고칠 방향: `test/trade-query.test.js`에 검사를 더한다 — `keys`가 있는 프리셋의 모든 키가 `MAP_MODS`의 `ids.join(',')` 집합에 있는지, `keys: null`인 프리셋은 `rec` 플래그 개수가 0이 아닌지. `presets.js`에 `module.exports`가 없으므로 다른 파일과 같은 모양으로 한 줄 더한다.
- 우선순위: P1 (NFR-LEAGUE-04) · 요구사항 신설 필요: NFR-DEV-08 — "기본 프리셋의 stat id가 모드 목록과 맞는지 자동 검사한다."

### T9. 없어진 stat id로 나가는 검색의 실패가 로그인 문제로 안내된다
- 증상: 거래소가 거절하면 "HTTP 400 … 로그인 상태를 확인하세요"가 뜬다. 조용히 무시한다면 더 나쁘다 — 검색은 성공하는데 그 모드만 안 걸러지고, 인게임 정규식은 계속 숨겨 "거래소는 안 걸러 주는데 인게임만 숨기는" 어긋남이 그대로 재현된다.
- 원인: `panel.js:935-938`이 상태 코드를 구분하지 않는다. 응답 본문도 `res.ok`일 때만 읽는다(`panel.js:942`).
- 고칠 방향: T4의 ②와 같은 자리. 실패일 때도 `body.error?.message`를 읽어 덧붙인다 — 거래소가 거부한 id를 알려 준다면 그 문구가 곧 갱신할 자리를 가리킨다.
- 우선순위: P1. ⚠ 확인 필요: 모르는 stat id를 거절하는지 무시하는지(V2). 무시한다면 P0로 올라가고, 고칠 곳은 안내가 아니라 추출 절차의 검사가 된다.
- 요구사항: FR-BUILD-12 보강.

### T10. 지난 리그의 북마크·기록이 구분되지 않는다
- 증상: 목록에 지난 리그와 현재 리그 항목이 똑같이 섞여 있다. 리그 이름은 메타 줄에 붙지만(`panel.js:329`·`453`) API의 영문 id라 드롭다운의 현지 이름과 표기도 다르다. 눌러 들어가면 조건이 안 뜨는데 사이드바는 "이미 저장된 검색입니다"라고 말한다(`panel.js:590`).
- 원인: 확장이 리그의 유효 기간을 모른다. `loadLeagues`의 결과는 빌더만 쓰고 목록 렌더링(`panel.js:318`·`445`)은 참조하지 않는다.
- 고칠 방향: 리그 목록을 목록 렌더링에서도 쓰고, `bookmark.league`가 목록에 없으면 메타 줄에 `· 지난 리그`를 붙인다. 지우지는 않는다. 목록을 아직 못 받았으면 아무 표시도 하지 않는다. 초기화 순서가 걸린다 — `renderList`(`panel.js:1295`)가 `initBuilder`(`panel.js:1298`)보다 먼저 돈다.
- 우선순위: P2 · ⚠ 확인 필요: 만료된 검색 ID와 없어진 리그에서 거래소가 무엇을 그리는지(V3) · 요구사항 신설 필요: FR-BM-11 — "지난 리그의 북마크·기록은 그 사실을 알아볼 수 있게 표시한다."(NFR-LEAGUE-05 구체화)

### T11. 8모드 북마크를 새 리그로 옮기는 길이 네 걸음이다
- 증상: 목록의 **정규식** 버튼으로 복사(`panel.js:333-345`) → 빌더의 정규식 칸에 붙여넣기 → **적용**(`panel.js:1209`·`1245`) → 리그 고르고 **거래소 검색 만들기**. 이 경로가 있다는 것을 README도 요구사항도 적지 않았다.
- 원인: 기능이 없는 것이 아니라 FR-BM-08과 FR-REGEX-03이 서로를 모른다.
- 고칠 방향: 가벼운 쪽은 README에 이 네 걸음을 적는 것. 무거운 쪽은 `regex`를 가진 북마크에 **빌더로 보내기** 버튼을 두는 것 — 새 UI라 요구사항을 먼저 세워야 한다.
- 우선순위: P2 · 요구사항: FR-BM-08 보강(문서 쪽) 또는 신설(버튼 쪽).

### T12. 문서가 저장소의 현재 상태와 어긋난 곳

| 자리 | 어긋남 | 고칠 방향 |
| --- | --- | --- |
| `CLAUDE.md` | `docs/DESIGN.md`와 `docs/design/adr.md`를 순서의 2단계로 가리키는데 **둘 다 없다**(`docs/design/`은 빈 디렉터리) | 설계 문서를 만들거나, 만들기 전까지 그 사실을 CLAUDE.md에 적는다. T7이 여기에 얹힌다 |
| `test/fixtures/README.md:93` | `data/search-mods.json`을 "추적하지 않음"이라 적었지만 `git ls-files`에 있다 | 문구를 지운다 |
| `README.md:323` | `test/real-map.js`를 가리키는데 그런 파일이 없다(`test/fixtures/real-maps.json`이다) | 경로를 고친다 |
| `docs/REQUIREMENT.md:149` (FR-BUILD-06) | "origin별로 24시간 캐시"라 적었지만 구현은 `leagues` 키 하나에 `{origin, at, leagues}`를 넣고 origin이 다르면 버린다(`panel.js:888-898`) | 두 서버를 오가는 일이 드무니 **문구를 구현에 맞추는 쪽**을 권한다 |

- 우선순위: P2 (첫 줄만 P1).

---

## 2. 구현과 요구사항의 어긋남

### B1. 옛 북마크에 요약을 나중에 채우는 경로가 죽어 있다
- 증상: 요약 없이 저장한 북마크는 **이름을 실제로 바꿔야만** 요약이 붙는다. 같은 이름으로 다시 저장해 채우는 길이 없다.
- 원인: 요약 보강이 이름 변경 분기 안에 있고(`panel.js:649`) 그 앞에서 이름이 같으면 조기 반환한다(`panel.js:642`). 버튼도 같은 이름이면 잠긴다(`panel.js:559`).
- 고칠 방향: 요약만 달라진 경우를 저장할 값으로 쳐서, `syncButton`의 잠금 조건과 제출부의 조기 반환 조건 양쪽에 "저장분에 요약이 없고 지금은 있다"를 더한다.
- 우선순위: P1 · FR-BM-10(현재 사실상 미구현), FR-BM-04.

### B2. 조건이 바뀌어도 추천 이름이 같으면 요약이 옛것으로 남는다
- 증상: 조건을 고쳤는데 추천 이름이 같으면 `currentSummary`가 갱신되지 않는다. 그대로 저장하면 툴팁 요약이 실제 조건과 어긋난다.
- 원인: `panel.js:545` — 이름만 비교해 조기 반환하고, `currentSummary` 대입은 그 아래(`547`)에 있다.
- 고칠 방향: 요약을 먼저 갱신하고 이름 비교는 표시 갱신에만 쓴다. 이름이 같아도 요약 구조가 달라졌으면 저장분이 따라가야 한다.
- 우선순위: P1 · FR-SUM-10 위배.

### B3. 다른 탭에서 북마크를 지우면 추천 이름이 멈춘다
- 증상: 다른 탭의 삭제가 반영된 뒤부터 주소가 바뀔 때까지 추천 이름이 더는 갱신되지 않는다.
- 원인: `storage.onChanged`가 `renderForm()`을 부르고(`panel.js:715`) 거기서 `filledName`이 `null`로 돌아가는데(`panel.js:569`) 재채움(`fillFromSearchPane`)이 이어지지 않는다. `watchSearch`는 `filledName === null`이면 그대로 돌아간다(`panel.js:541`).
- 고칠 방향: `refresh`가 하는 것처럼(`panel.js:618`) `renderForm` 뒤에 재채움을 잇거나, `filledName` 초기화를 `refresh` 쪽으로만 남긴다.
- 우선순위: P2 · FR-SUM-08 약화.

### B4. 이미 있는 URL이면 빌더 정규식이 저장되지 않는다
- 증상: 빌더로 만든 검색의 URL이 이미 북마크에 있으면 정규식이 저장되지 않고 보관분(`pending`)도 지워지지 않는다.
- 원인: `built`가 신규 추가 분기에서만 만들어지고 쓰인다(`panel.js:654`·`666`·`670`). 이름 변경 분기는 `built`를 모른다.
- 고칠 방향: `built` 판정을 두 분기 위로 올려, 이름 변경 때도 `regex`를 채우고 `clearPending()`을 부른다.
- 우선순위: P1 · FR-BUILD-08의 구멍.

### B5. 사이드바의 복사 두 곳이 거부되면 조용히 끝난다
- 증상: 클립보드가 거부되면(포커스 없음·권한) 아무 표시 없이 끝나거나 "복사됨"만 뜬다. EN 복사에는 폴백이 있다(`copy-en.js:68`의 `copyText`).
- 원인: `panel.js:340`(목록의 정규식 버튼)과 `panel.js:1252`(빌더의 복사 버튼)가 `navigator.clipboard.writeText`를 try/catch 없이 부른다.
- 고칠 방향: `copy-en.js`의 `copyText`와 같은 폴백을 사이드바에서도 쓸 수 있게 공용으로 올리고, 실패하면 안내 문구를 남긴다.
- 우선순위: P2 · 요구사항 신설 필요: FR-BUILD-17 — "사이드바의 클립보드 복사도 거부되면 대체 방식으로 다시 시도하고, 끝내 실패하면 알린다."(FR-EN-09와 같은 취지)

### B6. 폼을 못 읽는 페이지에서 마지막 대기 0.25초가 낭비된다
- 증상: 요약을 끝내 못 읽는 페이지에서 약 2초를 소진한다. 그 사이 사용자가 이동하면 검색 기록이 아예 안 쌓인다.
- 원인: `panel.js:495-503` — 마지막 시도 뒤에도 `sleep`한 다음 반복문을 빠져나간다.
- 고칠 방향: 마지막 회차에서는 쉬지 않고 끝낸다. 총 대기는 1.75초가 된다(FR-SUM-07의 "8회"는 그대로).
- 우선순위: P2.

### B7. 만료된 보관분이 저장소에 남는다
- 증상: 10분이 지난 `pending`이 지워지지 않고 계속 남는다.
- 원인: `panel.js:774-777` — 만료면 `null`을 돌려주기만 하고 지우지 않는다.
- 고칠 방향: 만료를 확인한 자리에서 `clearPending()`으로 함께 지운다.
- 우선순위: P2 · FR-SPA-04.

---

## 3. 정리할 것

### B8. `poe-regex.js`의 `inClass`가 두 번 정의되어 있다
- 원인: `poe-regex.js:437`과 `poe-regex.js:567` 둘 다 최상위 함수 선언이라, 나중 것이 앞의 것을 덮어 `437`은 도달 불가능한 죽은 코드다. 두 구현의 동작은 같아 증상은 없다.
- 고칠 방향: `437`을 지운다. 남는 `567`은 인자가 명령(`inst`)이라 호출부(`591`)와 이름이 맞는다.
- 우선순위: P2.

### B9. 주석이 코드와 어긋난 곳 다섯

| 자리 | 어긋남 | 고칠 방향 |
| --- | --- | --- |
| `search-summary.js:11` | 선택자를 `input.form-control.minmax`라 적었지만 실제는 `input.minmax`다(`52`·`115`) | 실제 선택자로 고친다 |
| `trade-url.js:14` | "manifest.json의 `host_permissions`와 함께 유지"라 적었지만 짝은 `content_scripts.matches`(와 `web_accessible_resources.matches`)다. `host_permissions`는 EN 복사용 `www.pathofexile.com` 하나뿐이다 | NFR-SEC-05의 표현으로 맞춘다 |
| `panel.js:162` | "`all: initial`에 지워지지 않도록"이라 적었지만 `all: initial`은 커스텀 속성을 초기화하지 않는다. 실제 근거는 폭 값의 출처를 `PANEL_WIDTH`(`panel.js:23`) 하나로 묶어 페이지를 밀어내는 값(`221`)과 같게 두는 것이다 | 실제 이유로 다시 쓴다 |
| `poe-regex.js:418` | "인게임 문법에는 역참조도 전방 탐색도 없어서"라 적었지만 전방 탐색은 지원한다(`17`·`284`·`494`, 테스트 있음). 근거는 **되추적하지 않는다**는 것이지 전방 탐색이 없다는 것이 아니다 | 역참조만 남기고 전방 탐색은 뺀다 |
| `map-mods.js:19-20` | "다섯 방향으로 대조한다"고 적고 다섯만 열거했지만 실제 대조는 일곱 방향이다(변종 전부, 거래소 stat id가 빠졌다) | FR-DATA-03과 같은 일곱 방향으로 고친다 |

- 우선순위: P2.

### B10. `MOD_GROUPS`에 소비처가 없다
- 원인: `map-mods.js:108`에 정의하고 `112`에서 export하지만 저장소 어디서도 읽지 않는다(증상은 없다). 창의 계열 제목은 각 모드의 `group`으로 만든다.
- 고칠 방향: 지우거나, 창의 계열 정렬 순서를 이 목록으로 고정해 쓰임을 준다. 지우는 쪽이면 export 줄도 함께 고친다.
- 우선순위: P2.

---

## 4. 나중에 생각해 볼 것

- **프리셋 내보내기/가져오기.** 지금은 `chrome.storage.local`에만 있어 다른 컴퓨터로 옮길 방법이 없습니다(REQUIREMENT §8의 "프리셋 이동 불가"). JSON 한 덩어리를 텍스트로 주고받는 정도면 파일 권한 없이도 됩니다.
- **북마크·기록까지 함께 내보내기.** 프리셋만 옮기면 반쪽이라 같은 자리에 붙이는 편이 자연스럽습니다.
