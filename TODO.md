# 할 일

리그 교체를 견디는 일과, 설계하다 발견한 구현·요구사항의 어긋남을 모아 둔 목록입니다.
각 항목은 증상 · 원인(파일:줄) · 고칠 방향만 적고 코드는 쓰지 않습니다. 기능을 손대기
전에 [docs/REQUIREMENT.md](docs/REQUIREMENT.md) → `docs/DESIGN.md` 순으로 먼저
고칩니다(CLAUDE.md). 순수한 버그 수정은 예외입니다.

## 우선순위 기준

기준은 **새 리그가 열린 날 사용자가 틀린 결과를 보는가**입니다. `⚠ 확인 필요`가 붙은
것은 실제 거래소에서 재 봐야 방향이 확정됩니다.

| 등급 | 뜻 |
| --- | --- |
| P0 | 새 리그가 열린 날 바로 사용자를 틀리게 만든다 |
| P1 | 그 주 안에 하지 않으면 갱신이 헛돌거나 사고를 못 잡는다 |
| P2 | 다음 리그까지 미뤄도 된다 |

---

## 1. 리그 교체 대응

### T4. 리그 이름이 형식이 다른 두 파일에 나뉘어 박혀 있다
- 증상: 리그 목록을 못 받은 날 쓰이는 이름이 두 곳에 흩어져 있어 한쪽만 고치고 넘어가기 쉽다. 그러면 드롭다운에는 새 리그가 뜨는데 등록은 끝난 리그로 나가거나 그 반대가 된다.
- 원인: `panel.js:788`의 `FALLBACK_LEAGUES`(`{id, text}` 배열)와 `trade-query.js:44`의 `DEFAULT_LEAGUE`(문자열 하나) — 파일도 형식도 다르다. `DEFAULT_LEAGUE`는 `trade-query.js`의 export에도 없어 테스트가 값을 읽을 수조차 없다.
- 고칠 방향: 한쪽으로 모으고 export에 올려 `node --test`가 두 값이 같은 리그를 가리키는지 검사하게 한다. (폴백 사용 알림과 상태 코드별 안내는 `c903836`에서 처리됨 — FR-BUILD-15, FR-BUILD-12 충족.)
- 우선순위: P1 (NFR-LEAGUE-01의 남은 부분).

### T6. 픽스처에 어느 리그·언제 받은 것인지가 없다
- 증상: 리그가 두어 번 지나면 `test/fixtures/`가 갱신된 것인지 알 근거가 없다. `node --test`는 사본끼리 맞춰 보므로 통과해도 무엇을 보증하는지 알 수 없다.
- 원인: 픽스처 JSON에 시점·출처 필드가 없고, `test/fixtures/README.md`도 출처만 적고 시점을 적지 않는다.
- 고칠 방향: 우선 `test/fixtures/README.md`와 `data/README.md`에 파일별 "받은 리그 / 받은 날 / 출처 URL" 표를 둔다. 더 강하게 하려면 `test/fixtures/meta.json`에 모아 테스트가 그 값을 출력하게 한다.
- 우선순위: P1 (NFR-LEAGUE-04) · 요구사항 신설 필요: NFR-DEV-06 — "픽스처는 어느 리그의 언제 받은 것인지를 함께 기록한다."

### T8. 기본 프리셋의 stat id를 검사하지 않는다
- 증상: `presets.js`의 키가 하나 낡으면 사용자가 프리셋을 고른 순간에야 "…1개는 건너뜀"으로 드러난다. 저장소의 데이터 오류를 사용자가 대신 발견한다.
- 원인: `test/`에 `presets.js`를 읽는 테스트가 없다. 키는 `modKey`(`panel.js:830`)와 글자 단위로 같아야 하는데 그것을 지키는 장치가 없다.
- 고칠 방향: `test/trade-query.test.js`에 검사를 더한다 — `keys`가 있는 프리셋의 모든 키가 `MAP_MODS`의 `ids.join(',')` 집합에 있는지, `keys: null`인 프리셋은 `rec` 플래그 개수가 0이 아닌지. `presets.js`에 `module.exports`가 없으므로 다른 파일과 같은 모양으로 한 줄 더한다.
- 우선순위: P1 (NFR-LEAGUE-04) · 요구사항 신설 필요: NFR-DEV-08 — "기본 프리셋의 stat id가 모드 목록과 맞는지 자동 검사한다."

### T9. 거래소가 모르는 stat id를 무시하는지 확인되지 않았다
- 증상: 조용히 무시한다면 검색은 성공하는데 그 모드만 안 걸러지고, 인게임 정규식은 계속 숨겨 "거래소는 안 걸러 주는데 인게임만 숨기는" 어긋남이 그대로 재현된다. 실패로도 안 드러나므로 사용자가 알아챌 길이 없다.
- 원인: 거절하는 경우는 이제 400으로 갈라 안내한다(`panel.js:1023-1025`). 무시하는 경우는 애초에 실패가 아니라서 안내로는 잡히지 않는다. 실패 응답 본문도 여전히 `res.ok`일 때만 읽는다(`panel.js:1059-1064`).
- 고칠 방향: V2를 실물에서 잰다. 무시한다면 고칠 곳은 안내가 아니라 `docs/DESIGN.md` §10.3 5단계 추출 명령의 검사다. 거절한다면 실패일 때도 `body.error?.message`를 읽어 덧붙인다 — 거래소가 거부한 id를 알려 준다면 그 문구가 곧 갱신할 자리를 가리킨다.
- 우선순위: P1. ⚠ 확인 필요: 모르는 stat id를 거절하는지 무시하는지(V2). 무시한다면 P0로 올라간다.
- 요구사항: FR-BUILD-12는 충족. NFR-LEAGUE-04의 남은 부분.

### T10. 지난 리그의 북마크·기록이 구분되지 않는다
- 증상: 목록에 지난 리그와 현재 리그 항목이 똑같이 섞여 있다. 리그 이름은 메타 줄에 붙지만(`panel.js:361`·`484`) API의 영문 id라 드롭다운의 현지 이름과 표기도 다르다. 눌러 들어가면 조건이 안 뜨는데 사이드바는 "이미 저장된 검색입니다"라고 말한다(`panel.js:642`).
- 원인: 확장이 리그의 유효 기간을 모른다. `loadLeagues`의 결과는 빌더만 쓰고 목록 렌더링(`panel.js:350`·`476`)은 참조하지 않는다.
- 고칠 방향: 리그 목록을 목록 렌더링에서도 쓰고, `bookmark.league`가 목록에 없으면 메타 줄에 `· 지난 리그`를 붙인다. 지우지는 않는다. 목록을 아직 못 받았으면 아무 표시도 하지 않는다. 초기화 순서가 걸린다 — `renderList`(`panel.js:1464`)가 `initBuilder`(`panel.js:1467`)보다 먼저 돈다.
- 우선순위: P2 · ⚠ 확인 필요: 만료된 검색 ID와 없어진 리그에서 거래소가 무엇을 그리는지(V3) · 미충족: NFR-LEAGUE-05 — "지난 리그의 북마크·기록·프리셋이 남아 있어도 오동작하지 않고, 만료된 검색은 사용자가 그 사실을 알아볼 수 있어야 한다."

### T11. 8모드 북마크를 새 리그로 옮기는 길이 네 걸음이다
- 증상: 목록의 **정규식** 버튼으로 복사(`panel.js:365-376`) → 빌더의 정규식 칸에 붙여넣기 → **적용**(`panel.js:1356`·`1394`) → 리그 고르고 **거래소 검색 만들기**. 이 경로가 있다는 것을 README도 요구사항도 적지 않았다.
- 원인: 기능이 없는 것이 아니라 FR-BM-08과 FR-REGEX-03이 서로를 모른다.
- 고칠 방향: 가벼운 쪽은 README에 이 네 걸음을 적는 것. 무거운 쪽은 `regex`를 가진 북마크에 **빌더로 보내기** 버튼을 두는 것 — 새 UI라 요구사항을 먼저 세워야 한다.
- 우선순위: P2 · 요구사항: FR-BM-08 보강(문서 쪽) 또는 신설(버튼 쪽).

### T12. 문서가 저장소의 현재 상태와 어긋난 곳

| 자리 | 어긋남 | 고칠 방향 |
| --- | --- | --- |
| `README.md:323` | `test/real-map.js`를 가리키는데 그런 파일이 없다(`test/fixtures/real-maps.json`이다) | 경로를 고친다 |
| `docs/REQUIREMENT.md:149` (FR-BUILD-06) | "origin별로 24시간 캐시"라 적었지만 구현은 `leagues` 키 하나에 `{origin, at, leagues}`를 넣고 origin이 다르면 버린다(`panel.js:972-986`) | 두 서버를 오가는 일이 드무니 **문구를 구현에 맞추는 쪽**을 권한다 |

- 우선순위: P2 (첫 줄만 P1).

---

## 2. 정리할 것

### T13. 사이드바의 클립보드 폴백을 덮는 요구사항이 없다
- 증상: 목록의 정규식 버튼과 빌더의 복사 버튼은 이제 `copyToClipboard`(`panel.js:296`)로 폴백과 실패 안내를 갖췄지만, 이 동작을 요구하는 줄이 `docs/REQUIREMENT.md`에 없다. 지워도 아무 기준에 걸리지 않는다.
- 원인: FR-EN-09가 EN 복사만 덮는다. 사이드바 복사는 `2d648c6`에서 코드가 먼저 갔다.
- 고칠 방향: FR-BUILD-17을 세운다 — "사이드바의 클립보드 복사도 거부되면 대체 방식으로 다시 시도하고, 끝내 실패하면 알린다."(FR-EN-09와 같은 취지)
- 우선순위: P2.

---

## 3. 나중에 생각해 볼 것

- **프리셋 내보내기/가져오기.** 지금은 `chrome.storage.local`에만 있어 다른 컴퓨터로 옮길 방법이 없습니다(REQUIREMENT §8의 "프리셋 이동 불가"). JSON 한 덩어리를 텍스트로 주고받는 정도면 파일 권한 없이도 됩니다.
- **북마크·기록까지 함께 내보내기.** 프리셋만 옮기면 반쪽이라 같은 자리에 붙이는 편이 자연스럽습니다.
