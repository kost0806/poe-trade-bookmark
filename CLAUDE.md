# CLAUDE.md

이 저장소에서 작업할 때 지켜야 할 것들입니다.

## 프로젝트

PoE1 거래소 검색을 북마크로 저장하고, 8모드 지도 검색과 인게임 정규식을 만들고,
아이템을 영문으로 복사하는 **크롬 확장**(Manifest V3). 순수 자바스크립트이고
빌드 단계도 의존성도 없습니다. 자세한 것은 [README.md](README.md).

## 문서가 먼저입니다

**기능을 고치거나 새로 넣을 때는 설계 문서를 먼저 고칩니다.** 코드부터 쓰고 문서를
나중에 맞추지 않습니다. 문서와 코드가 갈라지면 문서는 곧 아무도 안 믿는 글이 되고,
그때부터는 "왜 이렇게 되어 있는지"를 코드에서 매번 다시 알아내야 합니다.

순서:

1. **[docs/REQUIREMENT.md](docs/REQUIREMENT.md)** — 무엇을 해야 하는가. 사용자에게
   보이는 동작이 달라지면 해당 요구사항(`FR-…`/`NFR-…`)을 고치거나 새로 만듭니다.
   ID는 새로 붙이고 재사용하지 않습니다.
2. **[docs/DESIGN.md](docs/DESIGN.md)** 와 [docs/design/](docs/design/) — 어떻게 그렇게
   하는가. 구조·흐름·상태가 달라지면 여기부터 고칩니다. 되돌리기 어려운 결정을
   내렸다면 [docs/design/adr.md](docs/design/adr.md)에 그 결정을 남깁니다.
3. **코드** — 위 둘이 정한 대로 씁니다.
4. **테스트** — `node --test`. 데이터(맵모드·정규식 키워드)를 건드렸다면 대조 검사가
   같이 돌아야 합니다.

예외는 둘입니다.

- **버그 수정**: 문서가 정한 동작을 코드가 못 지키고 있는 경우입니다. 코드를 고치고
  문서는 그대로 둡니다. 다만 고치다 보니 **문서 쪽이 틀렸다면** 그 자리에서 문서를
  고치고, 어느 쪽을 정답으로 삼았는지 커밋 메시지에 적습니다.
- **문서에 영향이 없는 변경**: 오타, 주석, 서식, 테스트 보강.

작업을 마칠 때 **요구사항 → 설계 → 코드 → 테스트가 한 줄로 이어지는지** 확인합니다.
설계에 없는 동작이 코드에 있으면 둘 중 하나가 잘못된 것입니다.

## 개발 규칙

| 규칙 | 이유 |
| --- | --- |
| 의존성을 추가하지 않는다 (`package.json` 없음, `node_modules` 없음) | 압축 해제 로드로 바로 쓰는 확장이다. 빌드 단계를 들이면 그 성질을 잃는다 |
| 파일을 추가하면 `manifest.json`의 `content_scripts.js`에 **순서에 맞게** 넣는다 | 콘텐츠 스크립트는 `require` 없이 로드 순서대로 전역을 나눠 쓴다. 순서가 곧 의존 선언이다 |
| 거래소 DOM에 기대는 선택자는 해당 파일 맨 위에 모은다 | 거래소가 마크업을 바꿨을 때 고칠 곳이 한 군데여야 한다 |
| 정규식 키워드를 고치거나 추가하면 대조 검사를 돌린다 | 키워드 하나가 엉뚱한 지도를 인게임에서 숨긴다. 사람 눈으로는 못 잡는다 |
| 사이트를 추가하려면 `manifest.json` 두 곳과 `trade-url.js`의 `TRADE_HOSTS`를 함께 고친다 | 하나만 고치면 조용히 반쪽만 동작한다 |

## 코드 스타일

- 주석은 한국어, 평서체(`~한다`). **무엇을 하는지가 아니라 왜 그런지**를 적습니다.
  코드를 읽으면 아는 것은 주석으로 쓰지 않습니다.
- 문서는 한국어, 본문 존댓말(`~합니다`), 표 안은 평서체.
- 사용자에게 보이는 문구도 한국어입니다.

## 검증

```
node --test          # 정규식 엔진 · 쿼리 생성 · 검색 요약
node --check panel.js
```

브라우저가 있어야 확인되는 것(실제 거래소 마크업, 실물 매물, 인게임 검색창)은
자동으로 막지 못합니다. 그 경계와 확인 항목은 [docs/REQUIREMENT.md](docs/REQUIREMENT.md) §7에
있습니다.
